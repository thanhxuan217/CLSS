# requirements.txt — CLSS (MinHashRAG + Sino-Nom NER)
# Cleaned up from the original 151-package file.
# Removed: pytorch-pretrained-bert, pytorch-transformers (deprecated),
#          tensorflow, mxnet, stanfordnlp, Flask, allennlp (not used for Sino-Nom).
# Added: datasketch (MinHash LSH RAG)

--extra-index-url https://download.pytorch.org/whl/cu124
torch>=2.0.0,!=2.0.1,!=2.1.0
torchvision>=0.15.0
torchaudio>=2.0.0
transformers>=4.30.0
numpy<2.2.0,>=1.24.0
scipy<1.17.0,>=1.10.0
scikit-learn<1.9.0,>=1.2.0

# ── MinHash RAG (new) ──────────────────────────────────────────────────────────
datasketch>=1.6.0

# ── QLoRA (LoRA + 4-bit quantization) ─────────────────────────────────────────
peft>=0.7.0
bitsandbytes==0.48.0
nvidia-nvjitlink-cu12

# ── NLP Utilities ──────────────────────────────────────────────────────────────
tokenizers>=0.13.0
sentencepiece>=0.1.99
sacremoses>=0.0.53
segtok>=1.5.7
ftfy>=6.0.0
regex>=2022.1.18
nltk>=3.7
conllu>=4.4

# ── Embeddings & Word Vectors ──────────────────────────────────────────────────
gensim>=4.2.0
bpemb>=0.3.4

# ── Data & IO ─────────────────────────────────────────────────────────────────
h5py>=3.8.0
PyYAML>=6.0
tabulate>=0.9.0
pandas==2.2.2
tqdm>=4.65.0
boto3>=1.26.0

# ── Logging & Monitoring ───────────────────────────────────────────────────────
tensorboardX>=2.6.0
gpustat>=1.0.0
matplotlib>=3.7.0

# ── Utilities ─────────────────────────────────────────────────────────────────
Deprecated>=1.2.13
overrides>=6.0.0
editdistance>=0.6.0
smart-open>=6.0.0
filelock>=3.9.0
packaging>=23.0
requests>=2.28.0
Unidecode>=1.3.0

pyhocon>=0.3.59

# ── Testing ────────────────────────────────────────────────────────────────────
pytest>=7.0.0

# ── Optional / Extra Dependencies ──────────────────────────────────────────────
IPython>=7.0.0
psutil>=5.9.0
pyarrow>=10.0.0
sqlitedict>=2.1.0
hyperopt>=0.2.7
langdetect>=1.0.9
mpld3>=0.5.9
bert-score>=0.3.13
